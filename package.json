{
  "name": "encrypts3bucket",
  "version": "1.0.0",
  "description": "Encrypts all objects in a bucket by setting the encryption flag",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dan Sargeant",
  "license": "ISC",
  "devDependencies": {
    "async": "^3.2.0",
    "aws-sdk": "^2.814.0"
  }
}
